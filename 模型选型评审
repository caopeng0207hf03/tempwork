模型选型评审（端侧约束：INT8量化 + C语言实现 + SOC算力紧张，避免大模型）

一、总体结论
- 单位说明：FLOPs/OPs 是"单次推理的运算量"（一个计数，无时间维度，INT8场景下严格说应叫 OPs/MACs）；TOPS 是"每秒可执行的运算次数"（吞吐率，含时间维度，描述硬件算力或部署所需算力）。两者关系：所需算力(OPS/s) ≈ 单次推理OPs × 每秒推理次数。表格"单次推理FLOPs"列用FLOPs/OPs是对的；"部署算力需求"列用TOPS也是对的——因为它由延迟预算(如≤5ms)决定，本质是吞吐率，所以不能用FLOPs表示。
- 多数需求选型基本合理，可直接落地：#3 卡顿根因、#4 PDCCH、#5 HARQ、#6 移动速度。
- 需要简化/改选的有 3 项：#1 DCI、#2 CDRX、#7 服务质量（去掉C语言难实现或冗余的结构）。
- 必须改选的 1 项：#8 优选天线（DQN/PPO 过大，端侧不可行，改为轻量在线决策）。
- 通用原则：优先纯卷积/GRU/MLP 等C语言易实现、易量化的结构；避免 Multi-Head Attention、DeepFM 稀疏交互、大状态强化学习等在SOC上算力与实现成本高的结构。

二、逐项评审与重评估（仅列调整项）

#1 DCI大颗粒度预测
- 关于LSTM vs GRU：两者效果差别很小（GRU少一个门、参数约少25%、推理略快、C实现略简单），LSTM完全可保留。真正建议改的是 Multi-Head Attention（C语言实现与量化复杂、参数偏大）。
- 调整：LSTM 或 GRU 均可（2层×96~128）；注意力部分简化为单层轻量注意力或时序池化；保留场景Embedding。
- 重评估：参数规模 30万~60万；单次推理 0.4M~0.8M OPs；INT8模型 0.3~0.6MB；部署算力需求 ~0.1 TOPS。优先级 P1 不变。

#2 CDRX窗DCI预测
- 问题：BiLSTM 需完整序列、双向计算翻倍，≤2ms 端侧偏紧；CRF可保留（C查表解码可行）。
- 调整：BiLSTM → 单向 GRU(2层×64) + CRF；去掉"轻量Transformer"备选。
- 重评估：参数规模 20万~50万；FLOPs 0.3M~0.7M；INT8模型 0.2~0.5MB；等效算力 ~0.08 TOPS。优先级 P1 不变。

#7 服务质量评分
- 问题：DeepFM 稀疏特征交互在C语言/量化下实现成本高，收益有限。
- 调整：保留 Multi-task DNN（共享3层 + 多头），去掉 DeepFM 分支，稀疏特征改为离线Embedding查表后送入DNN。
- 重评估：参数规模 15万~40万；FLOPs 0.2M~0.6M；INT8模型 0.1~0.4MB；等效算力 ~0.04 TOPS。优先级 P2 不变。

#8 优选天线（必须改选）
- 问题：DQN/PPO 5M~50M FLOPs、2~20MB，端侧 NPU 1~2 TOPS 需求过高，训练/部署成本与SOC算力严重不匹配。
- 调整：改为 Contextual Bandit（LinUCB / 轻量MLP策略，在线快速决策）作为主方案；如需序列决策再用小型 Dueling-DQN（MLP 2层×64，动作空间≤8）。
- 重评估：参数规模 5万~20万；FLOPs 0.2M~0.6M；INT8模型 0.1~0.4MB；算力 0.1~0.3 MACs(INT8)；内存带宽 ~0.5 MB/s；端侧 NPU 0.2 TOPS 即可，≤3ms 可满足；等效算力 ~0.05 TOPS。优先级 P1 不变。

三、保持原选型（无需修改）
- #3 卡顿根因：XGBoost(C查表) + 1D-CNN，结构与算力合理。
- #4 PDCCH：TCN 纯膨胀卷积，C语言与量化友好。
- #5 HARQ：GRU(1层×64)+FC，已极致轻量，符合 ≤1ms。
- #6 移动速度：LSTM(1层×48) 或 1D-CNN(3层×32) 均可，统一骨干下随骨干选择，算力无压力。

四、端侧总体算力小结
- 调整后所有需求等效INT8算力均 ≤0.1 TOPS（#8 由 0.5~2.0 TOPS 降至 ~0.05 TOPS），整体端侧负载显著下降，符合SOC算力紧张约束。

五、统一/通用化模型结构建议（降低C语言算子与量化验证成本）
- 出发点：8个专题里 #1、#2、#4、#5、#6 本质都是"短序列时序预测/分类"，#3、#7 是"特征→打分/分类"，#8 是"在线决策"。若各自用不同结构（LSTM/BiLSTM/TCN/DeepFM/DQN…），端侧要实现和量化的算子种类很多，验证成本高。建议收敛到一套通用骨干。

- 通用骨干方案："共享时序骨干 + 任务专用轻量头"
  - 骨干可统一用 LSTM/GRU 循环网络，也可用 1D-CNN/TCN，二者都能做统一骨干，按团队偏好二选一：
    - 选 LSTM/GRU（推荐用于本场景）：精度上限略高、对强时序依赖任务（#1 DCI、#2 CDRX、#5 HARQ）更友好；C语言只需实现一个LSTM/GRU cell（矩阵乘+门控逐元素运算），算子也很少。代价：含时间步循环依赖，无法在时间维并行，长序列延迟略高、量化需注意门控/状态的定点范围。本场景序列都很短（5~16），循环开销可忽略，故LSTM作统一骨干完全可行。
    - 选 1D-CNN/TCN：无循环依赖、并行友好、量化最成熟；强时序任务精度可能略低。
    - 建议：本批需求序列短、且多为强时序预测，统一用 LSTM/GRU 作骨干更合适；若后续出现长序列或对吞吐敏感的任务，再用TCN骨干。
  - 序列类任务（#1 DCI、#2 CDRX、#4 PDCCH、#5 HARQ、#6 速度）：统一 LSTM/GRU 骨干 + 不同输出头（回归头/分类头/CRF头）。#2 的CRF、#1 的场景Embedding作为可插拔模块。
  - 表格类任务（#3 卡顿、#7 服务质量）：复用同一套 FC/MLP 头（MLP 是骨干的特例，去掉循环层即可）；#3 的XGBoost部分单独用C查表，不进神经网络算子库。
  - 决策类任务（#8 优选天线）：用 MLP 策略头（与表格类共用FC算子）+ Contextual Bandit在线更新，不引入RL专用大网络。

- 统一后端侧只需维护一个最小算子库：① LSTM/GRU cell（或1D卷积，取决于骨干选择）② 全连接FC ③ 激活(ReLU/sigmoid/tanh/softmax) ④ Embedding查表。所有专题共用同一推理引擎与同一量化流程。

- 收益：算子实现量从"每专题一套"降到"全局一套"；量化/定点验证一次覆盖多专题；代码体积与维护成本显著下降；各任务差异仅体现在配置（层数、单元数、序列长度）与输出头。

- 取舍提示：LSTM作统一骨干的唯一注意点是循环依赖带来的延迟与状态量化；本场景序列短、延迟预算可满足，可放心采用。如个别任务（如#4 PDCCH 可接受50ms）追求更高吞吐，可单独切换为TCN骨干（算子库相应增加1D卷积）。

六、更新后的需求清单（保持原表格列式，统一LSTM/GRU骨干 + 任务头）
| 序号 | 需求名 | 需求描述 | 责任人 | 模型选型（网络结构） | 算力需求 | 参数规模 | 单次推理FLOPs评估 | 量化后端侧部署算力需求 | 优先级 |
|------|--------|----------|--------|----------------------|----------|----------|-------------------|------------------------|--------|
| 1 | DCI大颗粒度预测 | 预测下行控制信息（DCI）的大颗粒度特征，如调度间隔、聚合等级等，针对抖音等典型业务场景进行差异化建模，优化UE功耗与调度效率 | 东宁 | 统一LSTM/GRU骨干(2层×96~128) + 场景Embedding + 回归/分类头（用轻量时序池化替代Multi-Head Attention） | 中（实时推理 ≤5ms，端侧NPU或CPU部署） | 30万~60万 | 0.4M~0.8M OPs（LSTM 2层×128，输入序列长度10，特征维度64） | INT8量化后模型大小：0.3~0.6 MB；算力：0.2~0.4 MACs（INT8）；内存带宽：~2 MB/s；部署方案：端侧NPU 0.5 TOPS满足 ≤5ms；等效INT8算力需求：~0.1 TOPS | P1（高） |
| 2 | CDRX窗DCI预测 | 在CDRX（连接态非连续接收）机制下，预测UE唤醒窗口内DCI的出现时机与格式，减少无效监听以降低终端功耗 | 东宁 | 统一LSTM/GRU骨干(2层×64) + CRF标注头（替代BiLSTM，结合CDRX周期特征工程） | 中（嵌入终端协议栈，实时推理 ≤2ms） | 20万~50万 | 0.3M~0.7M OPs（LSTM 2层×64 + CRF解码，输入序列长度8） | INT8量化后模型大小：0.2~0.5 MB；算力：0.15~0.35 MACs（INT8）；内存带宽：~1.5 MB/s；部署方案：嵌入协议栈，NPU 0.3 TOPS或DSP加速；等效INT8算力需求：~0.08 TOPS | P1（高） |
| 3 | 卡顿根因识别 | 识别由物理层参数配置、无线环境恶化、天线寻优不当等因素导致的业务卡顿，需增加维测日志并构建卡顿标签体系 | 陈宜华 | XGBoost/LightGBM（C查表，独立于神经网络算子库） + 共享FC/MLP头(3层×64) 做深度特征归因 | 低~中（离线训练，推理云端或边缘） | 10万~50万 | 0.3M~1.0M OPs（XGBoost 200棵树 + MLP 3层×64，特征维度128） | INT8量化后模型大小：0.2~0.8 MB；算力：0.15~0.5 MACs（INT8），XGBoost树推理无MACs；内存带宽：~1 MB/s；部署方案：XGBoost用CPU查表，MLP部分NPU 0.2 TOPS；等效INT8算力需求：~0.1 TOPS（云端可离线） | P1（高） |
| 4 | PDCCH频域预测 | 预测PDCCH在频域上的占位模式与资源映射，辅助UE缩小盲检搜索空间，降低解码复杂度 | 钱坤 | 统一LSTM/GRU骨干(2层×64) + 回归头；若追求吞吐可选切TCN骨干（可接受50ms延迟） | 中（离线/准实时，可接受50ms级延迟） | 20万~60万 | 0.4M~1.0M OPs（LSTM 2层×64，输入序列长度16） | INT8量化后模型大小：0.2~0.6 MB；算力：0.2~0.5 MACs（INT8）；内存带宽：~1 MB/s；部署方案：离线/准实时，NPU 0.2 TOPS或CPU多线程；等效INT8算力需求：~0.05 TOPS | P3（低） |
| 5 | HARQ预测 | 预测HARQ-ACK/NACK反馈结果及重传概率，辅助MAC层提前调度决策，减少HARQ RTT带来的空口时延 | 张苏春 | 统一GRU骨干(1层×64) + FC二分类头（输入MCS、BLER历史、信道质量等特征） | 低（终端侧部署，推理 ≤1ms，极致轻量） | 10万~30万 | 0.1M~0.3M OPs（GRU 1层×64 + FC 2层，输入序列长度5，二分类） | INT8量化后模型大小：0.05~0.15 MB（极致轻量）；算力：0.05~0.15 MACs（INT8）；内存带宽：~0.3 MB/s；部署方案：L1层内嵌，DSP/VLIW专用指令 ≤1ms；等效INT8算力需求：~0.03 TOPS | P2（中） |
| 6 | 移动速度预测 | 基于多普勒频移、RSRP变化率、切换事件等特征，预测UE移动速度等级（静止/低速/中速/高速），辅助切换参数自适应 | 樊勇 | 统一LSTM/GRU骨干(1层×48) + 分类/回归多任务头 | 低（推理频率低，100ms级周期即可） | 5万~20万 | 0.05M~0.2M OPs（LSTM 1层×48，输入维度16） | INT8量化后模型大小：0.02~0.1 MB；算力：0.025~0.1 MACs（INT8）；内存带宽：~0.2 MB/s；部署方案：CPU软件推理即可，100ms周期无算力压力；等效INT8算力需求：~0.01 TOPS | P2（中） |
| 7 | 服务质量评分 | 综合空口质量、业务QoS指标、用户体验等多维度信息，构建服务质量评分模型，量化评估用户真实体验 | 王超 | 共享FC/MLP多任务头(3层共享 + 多头评分)（去掉DeepFM，稀疏特征改离线Embedding查表后送入） | 低~中（评分周期性计算，秒级延迟可接受） | 15万~40万 | 0.2M~0.6M OPs（MLP 3层共享特征 + 2头输出，特征维度64） | INT8量化后模型大小：0.1~0.4 MB；算力：0.1~0.3 MACs（INT8）；内存带宽：~0.5 MB/s；部署方案：CPU/NPU均可，秒级评分无延迟压力；等效INT8算力需求：~0.04 TOPS | P2（中） |
| 8 | 优选天线 | 基于信道状态、业务需求与终端能力，动态选择最优天线组合与波束赋形参数，提升MIMO系统吞吐量 | 孙建文、师家红 | Contextual Bandit（LinUCB/轻量MLP策略，在线决策） + 共享FC头；如需序列决策用小型Dueling-DQN(MLP 2层×64，动作空间≤8)（替代DQN/PPO大网络） | 中（推理需低延迟 ≤3ms，在线更新轻量） | 5万~20万 | 0.2M~0.6M OPs（MLP策略 2层×64，状态维度64~128，动作空间≤8） | INT8量化后模型大小：0.1~0.4 MB；算力：0.1~0.3 MACs（INT8）；内存带宽：~0.5 MB/s；部署方案：推理端侧NPU 0.2 TOPS即可，≤3ms满足；等效INT8算力需求：~0.05 TOPS | P1（高） |

注：序列类任务（#1/#2/#4/#5/#6）共用同一LSTM/GRU骨干，仅配置（层数/单元数/序列长度）与输出头不同；表格/决策类（#3/#7/#8）共用FC/MLP头；端侧最小算子库：LSTM/GRU cell、FC、激活、Embedding查表（#3 XGBoost用C查表独立实现）。
